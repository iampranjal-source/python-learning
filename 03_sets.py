e = set() #dont use s = {} as it will create an empty dict.

s = {1, 5, 56, 7, 7, 7, 7}

print(s) # When Python creates a set, it stores the elements using a structure called a hash table. The position of an element is determined by its hash value, rather than by the order in which you typed it.
